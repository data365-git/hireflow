export type ExportRow = Record<string, string | number | boolean | null | undefined>;

export type SectionBadge = {
  label: string;
  selectedBg: string;
  selectedText: string;
  selectedRing: string;
  checkBg: string;
};
